{
  "name": "my-personal-prettier-lint-configs",
  "private": true,
  "version": "0.0.0",
  "engines": {},
  "browserslist": [
    "chrome >= 89, edge >= 89, opera >= 89",
    "node "
  ],
  "type": "module",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx --cache",
    "lint:fix": "npm run lint --fix",
    "prettier": "prettier src tests",
    "prettier:fix": "npm run prettier --fix",
    "format": "npm run prettier:fix & npm run lint:fix"
  }
}
